# 第二空間互動點詳細程式代號與邏輯

## 📋 互動點清單

| 序號 | Hotspot ID | 名稱 | 座標 | 位置描述 |
|------|-----------|------|------|----------|
| 1 | `mirror` | 破碎的鏡子 | `[0.1, 0.2, 0.35, 0.65]` | 左側牆上（上方） |
| 2 | `mirror_shard_spot` | 地上的鏡片碎角 | `[0.08, 0.87, 0.11, 0.93]` | 左側下方（已縮小） |
| 3 | `duty_schedule` | 值班表 | `[-0.05, 0.3, 0.15, 0.5]` | 左側牆上（中間） |
| 4 | `beds` | 移動病床 | `[0.5, 0.5, 0.9, 0.8]` | 中央區域 |
| 5 | `password_panel` | 密碼盤 | `[0.7, 0.2, 0.9, 0.4]` | 右側牆上 |
| 6 | `door_702` | 702號病房 | `[0.35, 0.15, 0.65, 0.4]` | 中間上方 |

---

## 🔍 各互動點的詳細處理邏輯

### 1. 破碎的鏡子 (`mirror`)

**程式位置**：`app/play/[chapterId]/[sceneId]/page.tsx` (362-382行)

**處理方式**：特殊處理（在 `interactWithHotspot` 之前）

**觸發條件**：
- `hotspotId === 'mirror'`
- `scene?.id === 'ch1_sc2'`

**邏輯流程**：
```typescript
if (hotspotId === 'mirror' && scene?.id === 'ch1_sc2') {
  const state = engine.getState();
  
  // 檢查：是否已收集鏡片碎角
  if (state.inventory.includes('mirror_shard')) {
    // 情況A：已收集
    → 顯示：「破碎的鏡面映出你支離破碎的倒影。你已經撿起了地上的碎片，但鏡子本身依然破碎。」
    → 類型：narrator
  } else {
    // 情況B：未收集
    → 顯示：hotspot.hint
    → 文案：「破碎的鏡面映出你支離破碎的倒影。它把你還給你，但先把你撕碎。」
    → 類型：narrator
  }
  
  setRefreshKey(prev => prev + 1);
  return; // 不繼續執行 interactWithHotspot
}
```

**相關事件**：無（純提示）

**相關道具**：無

**相關標記**：無

---

### 2. 地上的鏡片碎角 (`mirror_shard_spot`)

**程式位置**：`app/play/[chapterId]/[sceneId]/page.tsx` (434-460行)

**處理方式**：特殊處理（在 `interactWithHotspot` 之前）

**觸發條件**：
- `hotspotId === 'mirror_shard_spot'`
- `scene?.id === 'ch1_sc2'`

**邏輯流程**：
```typescript
if (hotspotId === 'mirror_shard_spot' && scene?.id === 'ch1_sc2') {
  const state = engine.getState();
  
  // 檢查：是否已收集鏡片碎角
  if (state.inventory.includes('mirror_shard')) {
    // 情況A：已收集
    → 顯示：「你已經收集了這片鏡子碎片。」
    → 類型：narrator
    → 返回（不繼續）
  }
  
  // 情況B：未收集
  → 記錄互動：engine.addInteraction('mirror_shard_spot')
  → 觸發事件：engine.triggerEvent('pickup_mirror_shard')
  → 顯示對話：事件返回的 dialog
  → 添加道具：mirror_shard（在事件中處理）
  
  // 如果事件觸發失敗，顯示 hotspot 提示
  if (!result?.dialog) {
    → 顯示：hotspot.hint
    → 文案：「一小片鏡子碎片掉在地上，邊緣鋒利。」
    → 類型：narrator
  }
  
  setRefreshKey(prev => prev + 1);
  return; // 不繼續執行 interactWithHotspot
}
```

**相關事件**：
- `pickup_mirror_shard` (data/gameData.ts: 414-432行)
  - 需求：`hasInteracted('mirror_shard_spot')`
  - 效果：添加道具 `mirror_shard`，顯示獲得對話

**相關道具**：`mirror_shard`（鏡片碎角）

**相關標記**：無

---

### 3. 值班表 (`duty_schedule`)

**程式位置**：`app/play/[chapterId]/[sceneId]/page.tsx` (413-434行)

**處理方式**：特殊處理（在 `interactWithHotspot` 之前）

**觸發條件**：
- `hotspotId === 'duty_schedule'`
- `scene?.id === 'ch1_sc2'`

**邏輯流程**：
```typescript
if (hotspotId === 'duty_schedule' && scene?.id === 'ch1_sc2') {
  const state = engine.getState();
  
  // 檢查：是否已有便條
  if (state.inventory.includes('note')) {
    // 情況A：已有便條（重新閱讀）
    → 觸發事件：engine.triggerEvent('read_note')
    → 顯示對話：「你再次查看便條：\n\n「主任只碰同意書。\n主治執行，住院記錄，\n護理師執行，實習生觀察。\n\n等級：主任 > 主治 > 住院 > 護理師 > 實習生」」
    → 類型：narrator
    → 返回（不繼續）
  } else {
    // 情況B：沒有便條（首次獲得）
    → 記錄互動：engine.addInteraction('duty_schedule')
    → 觸發事件：engine.triggerEvent('read_duty_schedule')
    → 顯示對話：「獲得：值班表背面的便條\n\n用急促筆跡寫著：\n「主任只碰同意書。\n主治執行，住院記錄，\n護理師執行，實習生觀察。\n\n等級：主任 > 主治 > 住院 > 護理師 > 實習生」」
    → 類型：item
    → 添加道具：note（在事件中處理）
    → 返回（不繼續）
  }
  
  // 如果事件觸發失敗，顯示 hotspot 提示
  if (!result?.dialog) {
    → 顯示：hotspot.hint
    → 文案：「背面似乎有字。」
    → 類型：narrator
  }
  
  setRefreshKey(prev => prev + 1);
  return; // 不繼續執行 interactWithHotspot
}
```

**相關事件**：
- `read_duty_schedule` (data/gameData.ts: 433-451行)
  - 需求：`hasInteracted('duty_schedule')`
  - 效果：添加道具 `note`，顯示獲得對話
- `read_note` (data/gameData.ts: 452-469行)
  - 需求：`hasItem('note')`
  - 效果：顯示重新閱讀對話

**相關道具**：`note`（值班表背面的便條）

**相關標記**：無

---

### 4. 移動病床 (`beds`)

**程式位置**：`app/play/[chapterId]/[sceneId]/page.tsx` (328-360行)

**處理方式**：特殊處理（在 `interactWithHotspot` 之前）

**觸發條件**：
- `hotspotId === 'beds'`
- `scene?.id === 'ch1_sc2'`

**邏輯流程**：
```typescript
if (hotspotId === 'beds' && scene?.id === 'ch1_sc2') {
  const state = engine.getState();
  
  // 檢查1：是否有鏡片碎角
  if (!state.inventory.includes('mirror_shard')) {
    // 情況A：沒有鏡片碎角
    → 顯示：「每張病床上都有標籤，但字跡模糊不清。你需要工具才能看清上面的內容。」
    → 類型：narrator
    → 返回（不繼續）
  }
  
  // 檢查2：是否已揭示標籤
  if (!state.flags.beds_labels_revealed) {
    // 情況B：有鏡片碎角但未揭示標籤
    → 記錄互動：engine.addInteraction('beds')
    → 顯示：「病床上的標籤很模糊，看不清楚。你手中的鏡片碎角或許可以反射光線，讓你看清標籤上的字。」
    → 類型：narrator
    → 返回（不繼續）
  }
  
  // 情況C：已滿足所有需求
  → 觸發謎題：bed_arrangement
  → 返回（不繼續）
}
```

**相關事件**：
- `use_mirror_shard_on_beds` (data/gameData.ts: 471-489行)
  - 需求：`hasItem('mirror_shard')` + `hasInteracted('beds')`
  - 效果：設置標記 `beds_labels_revealed = true`，顯示揭示對話
  - 觸發方式：在背包中點擊鏡片碎角（需要先與病床互動）

**相關謎題**：
- `bed_arrangement` (data/gameData.ts: 530-550行)
  - 類型：`arrangement`
  - 需求：`hasItem('mirror_shard')` + `beds_labels_revealed === true`
  - 答案：`['主任', '主治', '住院', '護理師', '實習生']`

**相關標記**：
- `beds_labels_revealed`：標籤是否已揭示
- `beds_arranged`：病床是否已排列完成
- `door_702_open`：702號病房的門是否已打開

---

### 5. 密碼盤 (`password_panel`)

**程式位置**：`app/play/[chapterId]/[sceneId]/page.tsx` (403-411行)

**處理方式**：特殊處理（在 `interactWithHotspot` 之前）

**觸發條件**：
- `hotspotId === 'password_panel'`
- `scene?.id === 'ch1_sc2'`

**邏輯流程**：
```typescript
if (hotspotId === 'password_panel' && scene?.id === 'ch1_sc2') {
  // 直接觸發可選謎題
  → 觸發謎題：mirror_password
  → 返回（不繼續）
}
```

**相關謎題**：
- `mirror_password` (data/gameData.ts: 514-529行)
  - 類型：`input`
  - 需求：無（可選謎題）
  - 答案：`'REFLECT'`
  - 提示：「你站到特定位置，從鏡子裡看到牆上的塗鴉不是亂寫，而像某人臨死前留下的「倒著說」。從鏡子裡看密碼盤是反的。」

**相關標記**：
- `mirror_solved`：鏡子反射謎題是否已解決

---

### 6. 702號病房的門 (`door_702`)

**程式位置**：`app/play/[chapterId]/[sceneId]/page.tsx` (384-401行)

**處理方式**：特殊處理（在 `interactWithHotspot` 之前）

**觸發條件**：
- `hotspotId === 'door_702'`
- `scene?.id === 'ch1_sc2'`

**邏輯流程**：
```typescript
if (hotspotId === 'door_702' && scene?.id === 'ch1_sc2') {
  const state = engine.getState();
  
  // 檢查：門是否已打開
  if (!state.flags.door_702_open) {
    // 情況A：門未打開
    → 顯示：「702號病房的門緊閉著，無法進入。」
    → 類型：narrator
    → 返回（不繼續）
  } else {
    // 情況B：門已打開
    → 顯示確認對話框：showDoor702Confirm
    → 選項：「進入」或「再等等」
    → 返回（不繼續）
  }
}
```

**相關標記**：
- `door_702_open`：門是否已打開（由病床排列謎題完成後設置）

---

## 🔄 處理順序（重要）

所有第二空間的互動點都**在 `interactWithHotspot` 之前**進行特殊處理，並使用 `return` 確保不會繼續執行通用邏輯：

```typescript
// 處理順序（從上到下）：
1. 病床 (beds) - 328行
2. 破碎的鏡子 (mirror) - 362行
3. 702號病房的門 (door_702) - 384行
4. 密碼盤 (password_panel) - 403行
5. 值班表 (duty_schedule) - 413行
6. 地上的鏡片碎角 (mirror_shard_spot) - 434行
7. 通用處理 (interactWithHotspot) - 462行
```

**關鍵點**：每個特殊處理都使用 `return`，確保不會執行 `interactWithHotspot`，避免重複觸發事件。

---

## 📝 事件系統對照表

| 事件ID | 觸發方式 | 需求 | 效果 |
|--------|---------|------|------|
| `pickup_mirror_shard` | 點擊 `mirror_shard_spot` | `hasInteracted('mirror_shard_spot')` | 添加道具 `mirror_shard` |
| `read_duty_schedule` | 點擊 `duty_schedule`（首次） | `hasInteracted('duty_schedule')` | 添加道具 `note` |
| `read_note` | 點擊 `duty_schedule`（再次）或背包中點擊便條 | `hasItem('note')` | 顯示重新閱讀對話 |
| `use_mirror_shard_on_beds` | 背包中點擊鏡片碎角（需先與病床互動） | `hasItem('mirror_shard')` + `hasInteracted('beds')` | 設置標記 `beds_labels_revealed` |
| `arrange_beds` | 病床排列謎題完成後自動觸發 | `beds_arranged === true` | 播放廣播，顯示旁白 |

---

## ⚠️ 避免混淆的關鍵設計

### 1. 每個互動點都有獨立處理

所有第二空間的互動點都在 `interactWithHotspot` **之前**進行特殊處理，並使用 `return` 確保不會繼續執行。

### 2. 明確的觸發條件

每個互動點都有明確的 `if` 條件檢查：
- `hotspotId === 'xxx'`
- `scene?.id === 'ch1_sc2'`

### 3. 避免事件重複觸發

特殊處理中直接調用 `engine.triggerEvent()`，不會再執行 `interactWithHotspot`，避免事件系統重複觸發。

### 4. 座標分離

- 值班表：`[-0.05, 0.3, 0.15, 0.5]`（左側中間）
- 鏡片碎角：`[0.08, 0.87, 0.11, 0.93]`（左側下方）
- 座標不重疊，但都使用特殊處理確保邏輯清晰

---

## 🎯 完整互動流程圖

```
進入第二空間
    ↓
【探索階段】
1. 點擊 mirror_shard_spot
   → 特殊處理 → 觸發 pickup_mirror_shard
   → 獲得：mirror_shard
    ↓
2. 點擊 duty_schedule
   → 特殊處理 → 觸發 read_duty_schedule
   → 獲得：note
    ↓
3. 點擊 mirror
   → 特殊處理 → 顯示提示（根據是否有鏡片碎角）
    ↓
【解謎階段】
4. 點擊 beds
   → 特殊處理 → 檢查鏡片碎角
   → 特殊處理 → 檢查標籤是否揭示
   → 特殊處理 → 觸發謎題 bed_arrangement
    ↓
5. 背包中點擊 mirror_shard（需先與病床互動）
   → 觸發 use_mirror_shard_on_beds
   → 設置 beds_labels_revealed = true
    ↓
6. 再次點擊 beds
   → 特殊處理 → 觸發謎題 bed_arrangement
    ↓
【可選】
7. 點擊 password_panel
   → 特殊處理 → 觸發謎題 mirror_password
    ↓
【通關】
8. 點擊 door_702（需先完成病床排列）
   → 特殊處理 → 顯示確認對話框
```

---

## ✅ 修正後的改進

1. **分離處理**：每個互動點都有獨立的特殊處理邏輯
2. **避免混淆**：使用 `return` 確保不會執行通用邏輯
3. **明確條件**：每個互動點都有明確的觸發條件
4. **統一類型**：所有提示統一使用 `narrator` 類型
5. **清晰文案**：區分「獲得時」和「重新閱讀時」的文案

